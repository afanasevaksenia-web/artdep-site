export default function FilesPage() {
  return (
    <div className="rounded-xl border border-border bg-surface p-4 text-sm text-muted">
      Файлы (загрузка с камеры/галереи, фильтры по цеху/сцене/типу, continuity) — этап 3–4 по
      CLAUDE.md. Бакет <code>project-files</code> и таблица <code>files</code> с правами уже
      настроены.
    </div>
  );
}
